<div class="container">
  <div class="row mt-5 mb-4">
    <% if @reservation.errors.any? %>
      <%= @reservation.errors.full_messages %>
    <% end %>
    <h1 class="text-center">新規予約画面</h1>
    
    <div class="form mt-5">
      <%= form_with model: @reservation, local: true, class: 'form' do |f| %>
      
        <div class="row d-flex justify-content-center">
          <div class="form-group col-md-3 col-sm-5">
            <%= f.label :day, '日付' %>
            <%= f.text_field :day, class: 'form-control', value: @day, readonly: true %>
          </div>
      
          <div class="form-group col-md-3 col-sm-5">
            <%= f.label :time, '時間' %>
            <%= f.text_field :time, class: 'form-control', value: @time, readonly: true %>
          </div>
        </div>
      
        <%= f.hidden_field :end_time, value: @end_time %>
        <%= f.hidden_field :user_id, value: current_user.id %>
      
        <div class="submit">
          <%= f.submit value: '予約する', class: 'btn btn-primary mx-auto d-block mt-5' %>
        </div>
      
      <% end %>
    </div>
    <div>
      <p class="text-center pt-3">※上記の日時で予約を行います。予約後にお寺から確認のお電話を致します。</p>
    </div>
  </div>
</div>