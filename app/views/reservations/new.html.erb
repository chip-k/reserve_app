<div class="container">
  <div class="row col-md-10 clo-sm-11 mx-auto">
    
    <% if @reservation.errors.any? %>
      <%= @reservation.errors.full_messages %>
    <% end %>
    
    <h2 class="title text-center mt-5 mb-4">新規予約画面</h2>
    
    <div class="form mt-5">
      <%= form_with model: @reservation, local: true, class: 'form' do |f| %>
      
        <table class="table table-responsive user-reservation-table">
          <tbody>
            
            <tr class="field">
              <th><%= f.label :day, '日付' %></th>
              <td class="col-lg-3"><%= f.text_field :day, class: 'form-control', value: @day, readonly: true %></td>
              <td></td>
            </tr>
            
            <tr class="field">
              <th><%= f.label :time, '時間' %></th>
              <td><%= f.text_field :time, class: 'form-control', value: @time, readonly: true %></td>
              <td></td>
            </tr>
            
          </tbody>
        </table>
        
        <%= f.hidden_field :temple_id, value: @temple %>
        <%= f.hidden_field :end_time, value: @end_time %>
        <%= f.hidden_field :user_id, value: current_user.id %>
      
        <div class="submit text-center">
          <%= f.submit value: '予約する', class: 'btn btn-outline-primary mt-5' %>
        </div>
      
      <% end %>
    </div>
    
    <div>
      <p class="text-center pt-3">※上記の日時で予約を行います。予約後にお寺から確認のお電話を致します。</p>
    </div>
    
  </div>
</div>