<% if model.errors.any? %>
  <div class="alert">
    <h3>
      <%= pluralize(model.errors.count, "件のエラーが発生しました。") %>
    </h3>
    <ul>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>